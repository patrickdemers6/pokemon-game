//
// Created by patrick on 2/2/22.
//

#include "util.h"


/**
 * Return the lesser of two numbers.
 */
int min(int a, int b) {
	return a < b ? a : b;
}

/**
 * Return the greater of two numbers.
 */
int max(int a, int b) {
	return a > b ? a : b;
}
